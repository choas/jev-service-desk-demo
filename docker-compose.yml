# Standalone run: docker compose up --build, then open http://localhost:8000
services:
  jev:
    build: .
    restart: unless-stopped
    env_file: .env          # OPENROUTER_API_KEY
    ports:
      - "127.0.0.1:8000:8000"
